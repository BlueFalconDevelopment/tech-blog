---
interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<a
  href={`/tags/${tag}`}
  class="rounded-none border border-term-border px-2.5 py-1 text-xs font-medium text-term-fg-dim transition hover:border-term-border-strong hover:text-term-fg-bright"
>
  #{tag}
</a>
